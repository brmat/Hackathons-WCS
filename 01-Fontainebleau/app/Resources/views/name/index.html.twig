{% extends 'base.html.twig' %}

{% block body %}
    <h1>name list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for name in names %}
            <tr>
                <td><a href="{{ path('name_show', { 'id': name.id }) }}">{{ name.id }}</a></td>
                <td>{{ name.name }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('name_show', { 'id': name.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('name_edit', { 'id': name.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('name_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
